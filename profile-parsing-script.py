import os
import json
import csv

PROFILES_DIRECTORY = 'profiles/'


for file in os.listdir(PROFILES_DIRECTORY):
	# open file (JSON)
	# get number of followers
	# for every post
		# if isVideo == false and multipleImage == false:
			# get urlImage
			# get tags
			# get description
			# date (split into date and time)
			# get numberLikes
	
	# save array into csv into file named



